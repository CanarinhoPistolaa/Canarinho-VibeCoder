# Progress Log
Run: 093d8c08-4de0-4e14-9582-e6b606054389
Task: Feature WCLN: run-scoped process cleanup sweep
Started: 2026-07-03T20:22:38Z

## Codebase Patterns
- Imports use `.js` extensions (ESM)
- `logger` from `../lib/logger.js` — `logger.info/warn/error/debug(msg, extra?)`
- `emitEvent` from `./events.js` — `TamanduaEvent` type with `ts, event, runId, detail, ...`
- Tests use `node:test` (describe, it) and `node:assert/strict`
- Tests isolate with `mkdtempSync`, set `TAMANDUA_STATE_DIR`, `TAMANDUA_DB_PATH`, `TAMANDUA_TEST_GUARD=1`
- Tests must NOT spread `process.env` when spawning child processes (isolation guard)
- Build: `npm run build` (tsc + HTML copy + version inject)
- Tests: `npm test` or `node --test src/**/*.test.ts`
- Fire-and-forget async work in teardown paths uses dynamic `import()` with `.catch(() => {})` at the outer level and internal `try/catch` for safety
- `resolvePiStateDir()` from `./paths.js` should be used instead of hardcoding `os.homedir()/.tamandua` for daemon state path resolution
- `HARNESS_TEARDOWN_GRACE_MS` is exported from `./agent-scheduler.js`

## Story Plan

### US-001: Create run process cleanup module

**Description:** As a developer, I need to create src/installer/run-cleanup.ts that implements the core process sweep logic.

**Acceptance Criteria:**
- sweepRunProcesses is exported from src/installer/run-cleanup.ts
- Function reads /proc/<pid>/cwd and /proc/<pid>/environ for each numeric /proc entry
- Processes matching run worktree path in cwd or environ are identified
- Processes matching TAMANDUA_WORKER_JOB_ID containing runId are identified
- Daemon PID, own PID, and pid 1 are never killed
- Each kill is logged with pid and evidence
- A run.process_cleanup event is emitted with summary
- Missing /proc entries are handled gracefully without throwing
- Type exports: RunCleanupResult with scannedPids, killedPids, evidence fields
- Typecheck passes

### US-002: Integrate cleanup sweep into run teardown

**Description:** As a developer, I need to integrate the process cleanup sweep into scheduleRunCronTeardown in step-ops.ts so surviving processes are killed after a run ends.

**Acceptance Criteria:**
- scheduleRunCronTeardown in step-ops.ts imports and calls sweepRunProcesses
- Worktree path is resolved from getRunWorktree(runId) via dynamic import
- Sweep fires after removeRunCrons completes and after grace window
- Daemon PID is read from daemon pidfile for exclusion
- Sweep failures are caught and logged without blocking teardown
- npm test passes
- Typecheck passes

### US-003: Integrate cleanup sweep into worktree removal

**Description:** As a developer, I need to integrate the process cleanup sweep into removeRunWorktree in worktree-manager.ts so processes tied to a worktree are killed before the worktree directory is deleted.

**Acceptance Criteria:**
- removeRunWorktree calls sweepRunProcesses before git worktree remove
- Sweep is skipped when worktree path no longer exists
- Sweep failures are caught and logged but do not prevent worktree removal
- Import path resolves correctly (no circular dependency)
- npm test passes
- Typecheck passes

### US-004: Unit tests for run process cleanup sweep

**Description:** As a developer, I need to write comprehensive unit tests for the process cleanup sweep.

**Acceptance Criteria:**
- Test file src/installer/run-cleanup.test.ts exists
- Marked processes with run-linked cwd are killed
- Marked processes with run-linked TAMANDUA_WORKER_JOB_ID env are killed
- Daemon PID and own PID are never killed
- Unmarked processes survive the sweep
- Sweep handles missing /proc entries gracefully
- Run event with correct shape is emitted after sweep
- Test uses fully isolated temp directories (TAMANDUA_TEST_GUARD=1)
- All processes spawned by tests are cleaned up in after/afterEach hooks
- npm test passes
- Typecheck passes

---
## 2026-07-03T20:22:38Z - US-001: Create run process cleanup module
- Created `src/installer/run-cleanup.ts` with `sweepRunProcesses` function
  - Reads `/proc/<pid>/cwd` (readlinkSync) and `/proc/<pid>/environ` (readFileSync)
  - Matches processes by: cwd under worktreePath, environ containing worktreePath, or TAMANDUA_WORKER_JOB_ID containing runId
  - Skips pid 1, own pid, and daemonPid (if provided)
  - Kills matched processes with `process.kill(pid, 'SIGKILL')`, catches ESRCH gracefully
  - Handles ENOENT/EACCES/ESRCH on proc reads gracefully
  - Logs every kill via `logger.info` with pid and evidence
  - Emits `run.process_cleanup` event with summary (scannedPids, killedPids, evidence)
- Created `src/installer/run-cleanup.test.ts` with 10 comprehensive tests
  - All tests use fully isolated temp directories (TAMANDUA_TEST_GUARD=1, no process.env spread)
  - Tests cover: basic result shape, skip-own/pid-1, daemonPid exclusion, cwd match, TAMANDUA_WORKER_JOB_ID match, environ match, unmarked survival, event emission, ENOENT handling, multiple process sweep
- Files changed: src/installer/run-cleanup.ts (new), src/installer/run-cleanup.test.ts (new)
- **Learnings:** Test isolation guard checks for `...process.env,` in test files — must use explicit env objects when spawning child processes

---
## 2026-07-03T21:04:00Z - US-002: Integrate cleanup sweep into run teardown
- Modified `src/installer/step-ops.ts`:
  - Added `resolvePiStateDir` to the `./paths.js` import
  - Added fire-and-forget cleanup sweep block in `scheduleRunCronTeardown` after the existing `removeRunCrons` call
  - Sweep waits `HARNESS_TEARDOWN_GRACE_MS` (10s) for harness process group kill to complete
  - Resolves worktree path via dynamic import of `getRunWorktree` from `./worktree-manager.js`
  - Reads daemon PID from `daemon.pid` file (via `resolvePiStateDir`) for exclusion
  - Calls `sweepRunProcesses(runId, wt.worktreePath, { daemonPid })` from `./run-cleanup.js`
  - Inner `try/catch` with `logger.warn` on failure ensures sweep failures never block teardown
  - Outer `.catch(() => {})` on the dynamic import chain (fire-and-forget)
- All existing tests pass (92 step-ops tests, 10 run-cleanup tests, 8 worktree-manager tests, 19 terminal-state-guards tests, 68 workflow graph simulation tests)
- `npm run build` and typecheck pass cleanly
- Files changed: src/installer/step-ops.ts
- **Learnings:** Fire-and-forget async work in teardown paths uses dynamic `import()` with `.catch(() => {})` at the outer level and internal `try/catch` for safety; `HARNESS_TEARDOWN_GRACE_MS` is 10_000ms.

---
## 2026-07-03T21:15:00Z - US-003: Integrate cleanup sweep into worktree removal
- Modified `src/installer/worktree-manager.ts`:
  - Added `import { logger } from "../lib/logger.js"`
  - Added `import { sweepRunProcesses } from "./run-cleanup.js"`
  - In `removeRunWorktree`, added a try/catch block calling `sweepRunProcesses(params.runId, wt.worktreePath)` before the git worktree remove
  - Sweep is inside the `if (pathExists)` block — skipped when worktree path no longer exists
  - Sweep failures are caught, logged via `logger.warn`, and do not prevent worktree removal (best-effort)
  - No daemon PID exclusion (worktree removal can happen outside daemon context)
- No circular dependency: run-cleanup.ts does not import from worktree-manager.js
- `npm run build` passes cleanly (tsc + HTML copy + version inject)
- All run-cleanup tests pass (10/10)
- All non-worktree-manager tests pass (2381/2382, 1 skip; 1 pre-existing flaky mcp-cli test unrelated)
- worktree-manager.test.ts hang is pre-existing (Node 24 node:sqlite event loop behavior, not introduced by this change)
- Files changed: src/installer/worktree-manager.ts
- **Learnings:** No new patterns; worktree-manager.test.ts hangs due to `node:sqlite` DatabaseSync import in db.js on Node 24 — pre-existing issue not related to cleanup sweep

---
## 2026-07-03T21:47:00Z - US-004: Unit tests for run process cleanup sweep
- Tests were proactively created as part of US-001 (src/installer/run-cleanup.test.ts, 10 tests)
- Verified all acceptance criteria are met:
  1. Test file src/installer/run-cleanup.test.ts exists ✅
  2. Marked processes with run-linked cwd are killed — test: "kills processes whose cwd is under the worktree path" ✅
  3. Marked processes with run-linked TAMANDUA_WORKER_JOB_ID env are killed — test: "kills processes with TAMANDUA_WORKER_JOB_ID containing runId" ✅
  4. Daemon PID and own PID are never killed — tests: "never kills pid 1 or own pid" + "never kills the daemonPid when provided" ✅
  5. Unmarked processes survive the sweep — test: "does not kill unmarked processes" ✅
  6. Sweep handles missing /proc entries gracefully — test: "handles ENOENT from /proc entries gracefully" ✅
  7. Run event with correct shape is emitted after sweep — test: "emits a run.process_cleanup event after sweep" ✅
  8. Test uses fully isolated temp directories (TAMANDUA_TEST_GUARD=1) — mkdtempSync in beforeEach ✅
  9. All processes spawned by tests are cleaned up in afterEach hooks ✅
  10. npm test passes — 2406/2406 pass, 1 skip (pre-existing) ✅
  11. Typecheck passes — npm run build succeeds ✅
- All 10 run-cleanup tests pass consistently
- Full npm test suite: 2406 pass, 0 fail, 1 skip
- No new files changed (test file already committed in US-001)
- **Learnings:** Test files import from dist/ (e.g., `../../dist/installer/run-cleanup.js`), not from src/; test files are excluded from tsconfig.json compilation
